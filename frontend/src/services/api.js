// from flask import Flask, jsonify
// from flask_cors import CORS  # Import CORS

// app = Flask(__name__)
// CORS(app)  # Enable CORS for all routes

// @app.route('/get-gesture', methods=['GET'])
// def get_gesture():
//     # Your hand gesture logic here (e.g., using mediapipe)
//     return jsonify({"gesture": "some gesture data"})  # Return some gesture data as JSON

// if __name__ == '__main__':
//     app.run(debug=True)
